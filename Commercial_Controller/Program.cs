using System;
using System.IO;

namespace Commercial_Controller
{
    class Program
    {
        static void Main(string[] args)
        {
            //int scenarioNumber = Int32.Parse(args[0]);
            //Scenarios scenarios = new Scenarios();
            //scenarios.run(scenarioNumber);

            // string columnID = "1";
            // string elevatorID = "1";
            // int floorRequestButtonID = 1;
            // int callButtonID = 1;
            // int floor;

            // Door door1 = new Door(1, "closed");
            // Console.Write(door1.status);
            // for (int i = 0; i < 10; i++)
            // {
            // Console.Write("hello");
            // }
        }
    }
}
